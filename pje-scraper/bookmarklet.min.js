javascript:void( (async function FALAW_PJE_SYNC() { const SB_URL = 'https://yleofidqkimeanpuothv.supabase.co'; const SB_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlsZW9maWRxa2ltZWFucHVvdGh2Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NDQ3Njc1MiwiZXhwIjoyMDkwMDUyNzUyfQ.j5ORA0kTEogVuzsDHh3YvneWrZMVKqyFRCkUjo7EF1k'; const MESES = {janeiro:1,fevereiro:2,'março':3,abril:4,maio:5,junho:6, julho:7,agosto:8,setembro:9,outubro:10,novembro:11,dezembro:12}; function parseDataPJe(txt) { const m = txt.trim().match(/(\d{1,2})\s+de\s+(\w+)\s+de\s+(\d{4})\s+(\d{1,2}):(\d{2})/i); if (!m) return { data: null, horario: '' }; const mes = MESES[m[2].toLowerCase()]; if (!mes) return { data: null, horario: '' }; const data = `${m[3]}-${String(mes).padStart(2,'0')}-${String(m[1]).padStart(2,'0')}`; const horario = `${m[4].padStart(2,'0')}:${m[5]}`; return { data, horario }; } function parseTipo(txt) { const s = txt.toUpperCase(); let tipo = '', mod = ''; if (s.includes('INSTRUÇÃO') || s.includes('INSTRUCAO')) tipo = 'INSTRUÇÃO'; else if (s.includes('CONCILIAÇÃO') || s.includes('CONCILIACAO')) tipo = 'CONCILIAÇÃO'; else if (s.includes('UNA')) tipo = 'UNA'; else if (s.includes('INICIAL')) tipo = 'INICIAL'; if (s.includes('VIDEOCONFERÊNCIA') || s.includes('VIRTUAL')) mod = 'VIRTUAL'; else if (s.includes('PRESENCIAL')) mod = 'PRESENCIAL'; else if (s.includes('HÍBRIDA') || s.includes('HIBRIDA')) mod = 'HÍBRIDA'; return { tipo, mod }; } function makeId(proc, data, hora) { const raw = `${proc}|${data}|${hora}`; let h = 0x811c9dc5; for (let i = 0; i < raw.length; i++) h = Math.imul(h ^ raw.charCodeAt(i), 0x01000193) >>> 0; return 'pje-' + h.toString(16).padStart(8,'0'); } function extractTRT() { const m = location.hostname.match(/trt(\d+)/i); return m ? `TRT${m[1]}` : location.hostname; } let overlay = document.getElementById('_falaw_sync_overlay'); if (overlay) overlay.remove(); overlay = document.createElement('div'); overlay.id = '_falaw_sync_overlay'; overlay.style.cssText = 'position:fixed;top:16px;right:16px;z-index:99999;background:#0D2B5E;color:#fff;font-family:sans-serif;font-size:13px;padding:16px 20px;border-radius:8px;min-width:280px;box-shadow:0 4px 20px rgba(0,0,0,.4);line-height:1.6'; overlay.innerHTML = '<b>📅 Falaw — Sincronizando PJe…</b><div id="_falaw_log" style="margin-top:8px;font-size:11px;opacity:.8"></div>'; document.body.appendChild(overlay); const log = (msg) => { document.getElementById('_falaw_log').textContent = msg; console.log('[Falaw]', msg); }; const finish = (msg, ok) => { overlay.style.background = ok ? '#1B7A4E' : '#C62828'; overlay.innerHTML = `<b>${ok ? '✓' : '✗'} Falaw PJe Sync</b><div style="margin-top:8px;font-size:12px">${msg}</div> <button onclick="this.closest('#_falaw_sync_overlay').remove()" style="margin-top:10px;background:rgba(255,255,255,.2);border:none;color:#fff;padding:4px 12px;border-radius:4px;cursor:pointer;font-size:11px">Fechar</button>`; }; try { const today = new Date(); const future = new Date(today); future.setMonth(future.getMonth() + 6); const fmt = d => `${String(d.getDate()).padStart(2,'0')}/${String(d.getMonth()+1).padStart(2,'0')}/${d.getFullYear()}`; const dateInputs = document.querySelectorAll('input[type="date"], p-calendar input, .p-calendar input, input[placeholder*="Data"], input[name*="data"]'); if (dateInputs.length >= 2) { const setVal = (el, val) => { const nativeSetter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set; nativeSetter.call(el, val); el.dispatchEvent(new Event('input', { bubbles: true })); el.dispatchEvent(new Event('change', { bubbles: true })); }; setVal(dateInputs[0], fmt(today)); setVal(dateInputs[1], fmt(future)); const btnFiltrar = document.querySelector('button[title*="Filtrar"], button[title*="Pesquisar"], .fa-filter, .pi-filter'); if (btnFiltrar) btnFiltrar.closest('button')?.click(); log('Expandindo intervalo de datas… aguarde'); await new Promise(r => setTimeout(r, 2500)); } } catch(e) { } const trt = extractTRT(); const allRows = []; async function scrapePage() { await new Promise(r => setTimeout(r, 800)); const rows = document.querySelectorAll('table tbody tr, .p-datatable-tbody tr, [class*="datatable"] tbody tr'); log(`Lendo ${rows.length} linha(s) da página atual…`); for (const tr of rows) { const cells = tr.querySelectorAll('td'); if (cells.length < 4) continue; const col0 = cells[0].innerText.trim(); const linhas0 = col0.split('\n').map(s => s.trim()).filter(Boolean); const processoRaw = (linhas0[0] || '').replace(/^[A-Za-záéíóúÁÉÍÓÚçÇ]+\s+/, '').trim(); // remove prefixo "ATOrd " const partes = linhas0.slice(1).join(' '); let reclamante = '', reclamada = ''; const vs = partes.split(/\s+x\s+/i); if (vs.length >= 2) { reclamante = vs[0].trim(); reclamada = vs.slice(1).join(' x ').trim(); } else reclamante = partes; const col2 = cells[2]?.innerText?.trim() || ''; const { data, horario } = parseDataPJe(col2); if (!data) continue; const hoje = new Date().toISOString().slice(0,10); if (data < hoje) continue; const tipoRaw = cells[3]?.innerText?.trim() || ''; const { tipo, mod } = parseTipo(tipoRaw); const vara = cells[4]?.innerText?.trim() || ''; const id = makeId(processoRaw, data, horario); allRows.push({ id, data_audiencia: data, horario, tipo_audiencia: tipo, modalidade: mod, processo: processoRaw, vara, reclamante, reclamada, cliente: '', link: '', id_senha: '', tipo_responsabilidade:'', responsavel_conducao: '', advogado: '', testemunha_necessaria: false, testemunhas: '', observacoes_preposto: '', dados_preposto: '', comentarios: '', orientacao: '', status: 'agendada', origem: 'pje', updated_at: new Date().toISOString(), }); } } async function nextPage() { const btn = document.querySelector( '.p-paginator-next:not(.p-disabled), button[aria-label="Next Page"]:not(:disabled), .pi-chevron-right:not([disabled])' ); if (!btn) return false; const pageBtn = btn.closest('button') || btn; if (pageBtn.disabled || pageBtn.classList.contains('p-disabled')) return false; pageBtn.click(); return true; } await scrapePage(); for (let p = 1; p < 20; p++) { const hasNext = await nextPage(); if (!hasNext) break; log(`Lendo página ${p+1}…`); await scrapePage(); } if (!allRows.length) { finish('Nenhuma audiência futura encontrada na página.<br>Verifique se está na aba "Minhas Audiências" do PJe.', false); return; } log(`${allRows.length} audiências encontradas. Salvando no Falaw…`); try { const res = await fetch(`${SB_URL}/rest/v1/pauta_audiencias`, { method: 'POST', headers: { 'apikey': SB_KEY, 'Authorization': `Bearer ${SB_KEY}`, 'Content-Type': 'application/json', 'Prefer': 'resolution=merge-duplicates,return=minimal', }, body: JSON.stringify(allRows), }); if (!res.ok) { const err = await res.text(); finish(`Erro ao salvar: ${res.status} — ${err.slice(0,120)}`, false); return; } finish(`✓ <b>${allRows.length} audiências</b> de <b>${trt}</b> sincronizadas com sucesso!<br><small>Abra o painel Falaw para visualizar.</small>`, true); } catch(e) { finish(`Erro de rede: ${e.message}`, false); } })(); )