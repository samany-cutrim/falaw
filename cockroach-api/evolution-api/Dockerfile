# Evolution API — imagem oficial
# https://github.com/EvolutionAPI/evolution-api
# v1.8.2 (última da linha v1.x): roda sem banco de dados (DATABASE_ENABLED=false).
# NÃO subir para v2.x sem provisionar um Postgres — a v2 exige
# DATABASE_PROVIDER=postgresql + DATABASE_CONNECTION_URI e crasha sem isso.
FROM atendai/evolution-api:v1.8.2

EXPOSE 8080
