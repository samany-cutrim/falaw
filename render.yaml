# Render Blueprint — Falaw Advogados
# Os cron jobs foram migrados para Supabase Edge Functions (pg_cron) — gratuito.
# Agendamento configurado em supabase-setup.sql.
#
# Este arquivo pode ser mantido vazio ou usado para futuros serviços pagos.

services: []
